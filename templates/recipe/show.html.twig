{% extends 'base.html.twig' %}
{% block title %}Recipe : {{ recipe.title }}{% endblock %}

{% block body %}
    <h1>{{ recipe.title }}</h1>
    <p>
        {{ recipe.content | nl2br }}
    </p>
    <a href="{{ url('recipe.edit', {id: recipe.id}) }}">Edit</a>
{% endblock %}