{% extends 'admin/admin.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
<h1>{{ category.name }}</h1>
{{ form_start(form) }}
    <div class="mb-3">
        {{ form_row(form.name) }}
        {{ form_row(form.slug) }}
    </div>
{{ form_end(form) }}
{% endblock %}